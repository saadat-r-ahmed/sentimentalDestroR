version: "3.9"

services:
  destror:
    build: .
    env_file: .env
    volumes:
      - ./data:/workspace/data
      - ./results:/workspace/results
      - ./configs:/workspace/configs
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
