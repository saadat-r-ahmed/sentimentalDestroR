# @package _global_
attack:
  name: back_translation
  pivots: [en, hi]
  similarity_threshold: 0.7

dataset:
  names: [blp23, youtube, cognisenti, basa_cricket]
  split: test
  max_samples: null

model:
  victims: [banglabert, banglishbert, xlm-r, muril, indicbertv2]

run:
  seeds: [42, 1337, 2024]
  output_dir: results/raw_runs
  wandb: true
